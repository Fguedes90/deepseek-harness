// Context source projection types now live in contract/context-provenance.ts
// (the snapshot contract references them); re-exported here so existing
// consumers keep their import site.
export * from '../contract/context-provenance.ts'
